# Firebase Configuration
FIREBASE_PROJECT_ID=your-project-id

# FIREBASE_SERVICE_ACCOUNT can be either:
# 1. Path to JSON file (local development): ./firebase-service-account.json
# 2. Full JSON content (production/Render): {"type":"service_account",...}
FIREBASE_SERVICE_ACCOUNT=./firebase-service-account.json

# Server Configuration
PORT=3000
FRONTEND_URL=http://localhost:5173
